services:
  moneymate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: moneymate-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
